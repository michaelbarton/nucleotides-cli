#!/usr/bin/env ruby
$:.unshift File.join(File.dirname(__FILE__), '..', 'lib')

require 'rubygems'
require 'ncle/push_event'

exit_code, msg = NCLE::PushEvent.execute!

if exit_code == 0
  STDOUT.puts msg
  exit(exit_code)
else
  STDERR.puts msg
  exit(exit_code)
end
